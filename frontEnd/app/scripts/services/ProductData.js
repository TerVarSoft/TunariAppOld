'use strict';

tunariAppApp.factory('productData', ['Restangular','$http', function(Restangular){
    
    //var Product = Restangular.all('products');
    //var allProducts = Product.getList();
    return{
      /*  products: allProducts,
        getProduct: function(productName){
            console.log(this.products);
            return _.where(this.products, {name:productName})[0];*/
        };
}]);
